# Android
*.apk
*.aab
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/
*.iml

# Generado (se crea con: python3 tools/build_data.py)
# web/data.json SÍ se versiona: lo consume el build de Android y el workflow lo valida.

# Python
__pycache__/
*.pyc

# Sistema
.DS_Store
Thumbs.db

# La APK la publica el workflow en dist/Certf-debug.apk con `git add -f`
# (se versiona a propósito para poder descargarla sin depender de los artefactos).
dist/
